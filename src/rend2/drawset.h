#pragma once

// A draw set is a list of objects residing on the GPU. All will be drawn with a single pipeline.
// When drawing, it generates a 'visible draw set' which is a subset of the draw set that is visible.
